#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	char sam;

for (seg = 'a'; seg <= 'z'; seg++)
{
	putchar(sam);
}
	putchar('\n');
return (0);
}
