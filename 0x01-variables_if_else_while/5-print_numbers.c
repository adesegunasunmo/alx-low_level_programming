#include <stdio.h>

/**
 * main - Entry point
 * 
 * Return: Always 0 (Success)
 */
int main(void)
{
int segun;

for (segun = 0; segun < 10; segun++)
{
	printf("%d", segun);
}
printf("\n");
return (0);
}
