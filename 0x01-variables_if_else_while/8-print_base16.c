#include <stdio.h>
/**
 * main - Entry point
 * 
 * Return: Always 0 (Success)
 */
int main(void)
{
int c;
char a;
for (c = 48; c < 58; c++)
	putchar(c);
for (c = 'l'; a <= 'f'; a++)
	putchar(a);
putchar('\n');
return (0);
}
